import { describe, expect, it } from "bun:test"

describe("Initial Test Case", () => {
  it("should run initial test case", () => {
    const isSuccess = true;
    expect(isSuccess).toBe(true);
  })
})